apiVersion: v1
kind: Service

metadata:
  name: fleetflow-backend

spec:

  type: {{ .Values.backend.service.type }}

  selector:
    app: fleetflow-backend

  ports:

    - protocol: TCP

      port: {{ .Values.backend.service.port }}

      targetPort: {{ .Values.backend.containerPort }}